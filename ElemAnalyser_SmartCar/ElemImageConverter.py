import ElemImage
import cv2
import numpy as np
img = ElemImage.ElemImage()
class Converter:

    def __init__(self):
        pass

    def correction_keystoning(self):
        matrix = np.array([], dtype=np.uint8)
        if img.isRGB:
            pass
        if img.isgray:
            pass
        if img.isbin:
            pass